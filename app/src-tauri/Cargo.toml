[package]
name = "ozalid-studio"
version = "0.2.0"
edition = "2021"

[lib]
name = "ozalid_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
# Archive .ozalid : deflate pour le TOML et le manuscrit, stocké tel quel pour les
# images, déjà compressées. Aucune autre méthode n'est utile ici.
zip = { version = "7", default-features = false, features = ["deflate"] }
# Le bloc de réglages des PNG de l'atelier est du base64.
base64 = "0.22"
# Décoder une photo d'envoi pour en séparer l'encre du papier. `image.rs` ne lit que des
# en-têtes ; un décodeur JPEG ne se réécrit pas. Restreinte aux deux formats que
# l'application accepte, comme `zip` l'est à `deflate`.
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
# La seule dépendance réseau du projet, tirée par le seul envoi généré par diffusion :
# synchrone, sans exécuteur asynchrone à faire vivre, et avec ses propres racines de
# certification — l'application est empaquetée, elle ne peut pas compter sur celles du
# poste. Rien d'autre dans Ozalid n'ouvre de connexion.
ureq = "3"
tauri-plugin-opener = "2.5.4"

[dev-dependencies]
# Répertoires de travail jetables pour les tests d'import (qui touchent au disque).
tempfile = "3"

[profile.release]
strip = true
lto = true
codegen-units = 1
